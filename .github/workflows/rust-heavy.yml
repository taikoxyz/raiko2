name: rust-heavy

on:
  workflow_dispatch:
    inputs:
      lane:
        description: Heavy Rust lane to run
        required: true
        default: all
        type: choice
        options:
          - prover
          - engine
          - server
          - all
      ref:
        description: Branch to test; defaults to the branch this workflow was dispatched from
        required: false
        type: string

permissions:
  contents: read

concurrency:
  group: rust-heavy-${{ github.event.inputs.ref || github.ref }}-${{ github.event.inputs.lane || 'all' }}
  cancel-in-progress: false

jobs:
  resolve-ref:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    outputs:
      ref: ${{ steps.target.outputs.ref }}
    env:
      TARGET_REF_INPUT: ${{ inputs.ref }}
      DISPATCH_REF: ${{ github.ref_name }}
    steps:
      - name: resolve target ref
        id: target
        shell: bash
        run: |
          set -eu
          target_ref="$(printf '%s' "${TARGET_REF_INPUT}" | xargs)"
          if [ -z "${target_ref}" ]; then
            target_ref="${DISPATCH_REF}"
          fi
          echo "ref=${target_ref}" >> "${GITHUB_OUTPUT}"
          echo "Resolved target ref: ${target_ref}"

  test-prover:
    needs: resolve-ref
    if: inputs.lane == 'prover' || inputs.lane == 'all'
    runs-on: ubuntu-latest
    permissions:
      contents: read
    env:
      CARGO_TERM_COLOR: always
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4.3.1
        with:
          ref: ${{ needs.resolve-ref.outputs.ref }}
          persist-credentials: false

      - uses: dtolnay/rust-toolchain@29eef336d9b2848a0b548edc03f92a220660cdb8 # stable @ 2026-03-27
        with:
          toolchain: "1.94.0"

      - uses: rui314/setup-mold@9c9c13bf4c3f1adef0cc596abc155580bcb04444 # v1

      - name: print resolved revision
        run: |
          git rev-parse HEAD
          git branch --show-current

      - name: print boundless dependency path when present
        shell: bash
        run: |
          set -eu
          cargo tree -i boundless-market -p raiko2-prover || \
            echo "boundless-market is not in the default feature graph"

      - name: check prover smoke
        run: cargo check -p raiko2-prover -p guest-launcher --tests

      - name: dump boundless generated source on failure
        if: failure()
        shell: bash
        run: |
          set -eu
          if [ ! -d target/debug/build ]; then
            echo "target/debug/build missing; no boundless generated source to dump"
            exit 0
          fi
          find target/debug/build -path '*boundless_market_generated.rs' -print || true
          for f in $(find target/debug/build -path '*boundless_market_generated.rs' 2>/dev/null); do
            echo "FILE:${f}"
            sha256sum "${f}"
            sed -n '1,40p' "${f}"
            echo "---TAIL---"
            tail -n 40 "${f}"
          done

  test-engine:
    needs: resolve-ref
    if: inputs.lane == 'engine' || inputs.lane == 'all'
    runs-on: ubuntu-latest
    permissions:
      contents: read
    env:
      CARGO_TERM_COLOR: always
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4.3.1
        with:
          ref: ${{ needs.resolve-ref.outputs.ref }}
          persist-credentials: false

      - uses: dtolnay/rust-toolchain@29eef336d9b2848a0b548edc03f92a220660cdb8 # stable @ 2026-03-27
        with:
          toolchain: "1.94.0"

      - uses: rui314/setup-mold@9c9c13bf4c3f1adef0cc596abc155580bcb04444 # v1

      - name: print resolved revision
        run: |
          git rev-parse HEAD
          git branch --show-current

      - name: print boundless dependency path when present
        shell: bash
        run: |
          set -eu
          cargo tree -i boundless-market -p raiko2-engine || \
            echo "boundless-market is not in the default feature graph"

      - name: check engine smoke
        run: cargo check -p raiko2-engine --tests

      - name: dump boundless generated source on failure
        if: failure()
        shell: bash
        run: |
          set -eu
          if [ ! -d target/debug/build ]; then
            echo "target/debug/build missing; no boundless generated source to dump"
            exit 0
          fi
          find target/debug/build -path '*boundless_market_generated.rs' -print || true
          for f in $(find target/debug/build -path '*boundless_market_generated.rs' 2>/dev/null); do
            echo "FILE:${f}"
            sha256sum "${f}"
            sed -n '1,40p' "${f}"
            echo "---TAIL---"
            tail -n 40 "${f}"
          done

  test-server:
    needs: resolve-ref
    if: inputs.lane == 'server' || inputs.lane == 'all'
    runs-on: ubuntu-latest
    permissions:
      contents: read
    env:
      CARGO_TERM_COLOR: always
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4.3.1
        with:
          ref: ${{ needs.resolve-ref.outputs.ref }}
          persist-credentials: false

      - uses: dtolnay/rust-toolchain@29eef336d9b2848a0b548edc03f92a220660cdb8 # stable @ 2026-03-27
        with:
          toolchain: "1.94.0"

      - uses: rui314/setup-mold@9c9c13bf4c3f1adef0cc596abc155580bcb04444 # v1

      - name: print resolved revision
        run: |
          git rev-parse HEAD
          git branch --show-current

      - name: print boundless dependency path when present
        shell: bash
        run: |
          set -eu
          cargo tree -i boundless-market -p raiko2 || \
            echo "boundless-market is not in the default feature graph"

      - name: check server smoke
        run: cargo check -p raiko2 --tests

      - name: dump boundless generated source on failure
        if: failure()
        shell: bash
        run: |
          set -eu
          if [ ! -d target/debug/build ]; then
            echo "target/debug/build missing; no boundless generated source to dump"
            exit 0
          fi
          find target/debug/build -path '*boundless_market_generated.rs' -print || true
          for f in $(find target/debug/build -path '*boundless_market_generated.rs' 2>/dev/null); do
            echo "FILE:${f}"
            sha256sum "${f}"
            sed -n '1,40p' "${f}"
            echo "---TAIL---"
            tail -n 40 "${f}"
          done
